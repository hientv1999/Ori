# Ori partition table — 16 MB flash.
# nvs 20 KB | otadata 8 KB | two 3 MB OTA app slots | spiffs ~9.77 MB |
# factory 16 KB | coredump 64 KB
# All offsets 0x1000-aligned; app slots 0x10000-aligned.
#
# Layout:
#   bootloader + ptable : 0x000000 – 0x009000  (fixed by Arduino-ESP32)
#   nvs                 : 0x009000 – 0x00E000  (20 KB)
#   otadata             : 0x00E000 – 0x010000  (8 KB)
#   app0 (ota_0)        : 0x010000 – 0x310000  (3 MB)
#   app1 (ota_1)        : 0x310000 – 0x610000  (3 MB)
#   spiffs              : 0x610000 – 0xFEC000  (~9.77 MB — fonts, images, assets)
#   factory             : 0xFEC000 – 0xFF0000  (16 KB — write-once serial identity NVS,
#                          see provisioning.md. NEVER touched by nvs::factory_reset()
#                          or app OTA — a physically separate NVS partition so no
#                          future "wipe everything" bug can take it out.)
#   coredump            : 0xFF0000 – 0x1000000 (64 KB — panic/WDT crash dumps)
#
# NOTE: shrinking spiffs changes the LittleFS partition size, so it reformats
# once on next boot (synced profile/Time Off photos re-download from Orion).
# This happened once before for the coredump carve-out (was 64 KB larger) and
# again now for the factory partition below.
#
# Name,    Type, SubType,  Offset,   Size,
nvs,       data, nvs,      0x9000,   0x5000,
otadata,   data, ota,      0xE000,   0x2000,
app0,      app,  ota_0,    0x10000,  0x300000,
app1,      app,  ota_1,    0x310000, 0x300000,
spiffs,    data, spiffs,   0x610000, 0x9DC000,
factory,   data, nvs,      0xFEC000, 0x4000,
coredump,  data, coredump, 0xFF0000, 0x10000,
